import { styled } from '../../styles/theme'

export const ExplorerContainer = styled('main', {
  width: '100%',
  height: '100vh',

  display: 'flex',
})

export const Content = styled('div', {
  padding: '4.5rem 6rem',
  overflow: 'auto',
})
